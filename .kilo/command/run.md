---
description: Run QUATRA interpreter on a source file
agent: compiler-dev
subtask: true
---
Run `$1` with the QUATRA interpreter/VM.

1. Run: `cargo run -- run "$1"`
2. Show the interpreter output and any stack traces.
